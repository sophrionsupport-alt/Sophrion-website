/**
 * Canonical marketing layout — matches the homepage shell so gutters and max
 * width stay consistent across breakpoints (header, body, footer).
 */
export const marketingContainerClass =
  "mx-auto w-full max-w-7xl px-4 sm:px-6 lg:px-8";

/** Primary hero / lead column width (homepage hero). */
export const marketingHeroClass = "max-w-4xl";

/** Default vertical padding for top-of-page marketing heroes. */
export const marketingHeroSectionYClass = "py-16 sm:py-24";
